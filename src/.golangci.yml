version: "2"

run:
  timeout: 5m
  build-tags:
    - cli
  modules-download-mode: vendor
  tests: false

linters:
  default: standard
  enable:
    - gosec
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
